import React from "react";

const Header = props => (
	<div className="container instructions">
		<p>Click on each color once. If you click on the same color twice, you lose!</p>
	</div>
);

export default Header;